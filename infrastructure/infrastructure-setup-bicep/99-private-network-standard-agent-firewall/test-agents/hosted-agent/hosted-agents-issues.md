Okay, so I've been playing with hosted agents this morning and I want to just put down my thoughts so I don't forget anything so in no particular order number one. I've been using the C sharp API. The first thing that I noticed is that when I publish my hosted agent if the MCP tool calls require approval the hosted agent doesn't appear to work so it looks like the foundry sorry the agent framework rapper that is interpreting. The toolbox responses is not telling me that I'm being challenged for a approval so yeah that's not an passthrough that's just using the box standard approval stuff. I also had issues using all of passthrough as well. I was getting this ARA oboe token problem so I'm not sure what that was related to.

Okay so when I publish my agent things are pretty good at that point in time but I did find that everything other samples suggested that the permissions needed to be given to the project identity of foundry. It looks like now every agent gets its own agent identity so my hostage agent immediately failed because the agent identity for the agent needed AI project user on the foundry project itself so that was quite interesting as well. After that yeah look the first time you run an agent. It takes a few seconds. I'm guessing it's because it's pulling down the container, but they're really quick after that so taking me awhile to get to this point but that's pretty good.

Something is a bit weird with the telemetry as well for hosted agents so what I mean by that is whenever I look at a conversation inside the playground ever response seems to have a tool associated with it which just says message type and showed you the response to the chat even if the tool was never called. Also if I go and look at the overall conversation against the session I only see the responses I don't see any tool calls even if I know that a tool was called and I can see it all inside application insights. I can see MCP tools being called so I don't know. Something just doesn't seem to be quite adding up right for hosted agents.

